OPENAI_API_KEY=                # For Whisper transcription (gpt-4o-mini-transcribe at $0.003/min)
ANTHROPIC_API_KEY=             # For Claude Vision API + reasoning
GENSPARK_API_KEY=              # For external research (optional — Claude web search fallback used if absent)
ATLAS_INGESTION_PATH=          # Default folder path (can also be set via UI widget)
ATLAS_MAX_VISUALS_PER_FILE=20  # Cap on embedded images described per PDF/DOCX/PPTX (default 20)
